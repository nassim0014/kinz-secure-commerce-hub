# FastAPI backend dependencies — pinned for reproducible builds.
fastapi==0.138.2
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0
sqlalchemy==2.0.29
psycopg[binary]==3.3.4
# NOTE: python-jose removed (CVE-2024-33664 algorithm confusion,
#        CVE-2024-33663 JWT-bomb DoS, PYSEC-2025-185 JWE DoS).
#        PyJWT is the maintained, CVE-free alternative and is what
#        the codebase actually uses.
PyJWT==2.13.0
bcrypt==4.1.2
slowapi==0.1.9
python-multipart==0.0.32
pandas==2.2.1
APScheduler==3.10.4

# Dev / test
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.27.0
ruff==0.3.5
pip-audit==2.7.3
bandit==1.9.4

# Build-time toolchain, pinned only to satisfy `pip-audit --strict`.
# setuptools is not imported by this application. It ships ambiently in the
# Python 3.11 runner image (79.0.1, PYSEC-2026-3447) and pip-audit inspects the
# whole installed environment, so the 3.11 matrix leg failed while 3.12 passed —
# Python 3.12 venvs no longer bundle setuptools at all. Pinning it here upgrades
# the ambient copy on 3.11 and is a no-op on 3.12.
setuptools>=83.0.0

