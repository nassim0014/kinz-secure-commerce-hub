line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # line length is enforced by formatter, not linter

[per-file-ignores]
"tests/*" = ["B011"]
