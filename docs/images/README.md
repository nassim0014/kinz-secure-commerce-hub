# Image Placeholders

Drop the following screenshots here after running `docker compose up`:

- `dashboard-mockup.png` — main KPI dashboard view
- `channels-mockup.png` — channel breakdown chart
- `products-mockup.png` — product performance table
- `login-mockup.png` — login screen with audit-logged JWT auth
- `swagger-mockup.png` — auto-generated OpenAPI docs at `/docs`

The README references these paths directly so the repo is recruiter-ready
the moment the screenshots are added.
